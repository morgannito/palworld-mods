# Analyse : EasyPalExpedition.zip

mount point `../../../Pal/Content/Pal/DataTable/CharacterTeamMission/`

### `Pal/Content/Pal/DataTable/CharacterTeamMission/DT_CharacterTeamMissionDataTable.uasset`
- rows : 18 vanilla → 18 moddées (+0 / -0 / ~18 modifiées)
  - `Dungeon_Grass` : RequiredSeconds: 1800 → 60, RecommendedStrength: 25000 → 1000, ReleaseCondition: None → None, ChallengeCondition: DefeatGrassBoss → None
  - `Dungeon_Forest` : RequiredSeconds: 1800 → 60, RecommendedStrength: 56000 → 1000, ReleaseCondition: None → None, ChallengeCondition: DefeatForestBoss → None
  - `Dungeon_Volcano` : RequiredSeconds: 2700 → 60, RecommendedStrength: 144000 → 1000, ReleaseCondition: None → None, ChallengeCondition: DefeatVolcanoBoss → None
  - `Dungeon_Desert` : RequiredSeconds: 2700 → 60, RecommendedStrength: 209000 → 1000, ReleaseCondition: None → None, ChallengeCondition: DefeatDesertBoss → None
  - `Dungeon_Snow` : RequiredSeconds: 3600 → 60, RecommendedStrength: 286000 → 1000, ReleaseCondition: None → None, ChallengeCondition: DefeatSnowBoss → None
  - `Dungeon_Sakurajima` : RequiredSeconds: 3600 → 60, RecommendedStrength: 375000 → 1000, ReleaseCondition: None → None, ChallengeCondition: DefeatSakurajimaBoss → None
  - `Dungeon_DarkIsland` : RequiredSeconds: 3600 → 60, RecommendedStrength: 476000 → 1000, ReleaseCondition: None → None, ChallengeCondition: DefeatDarkIslandBoss → None
  - `Dungeon_SkyIsland` : RequiredSeconds: 3600 → 60, RecommendedStrength: 589000 → 1000, ReleaseCondition: None → None, ChallengeCondition: DefeatSkyIslandBoss → None
  - `Dungeon_WorldTree` : RequiredSeconds: 3600 → 60, RecommendedStrength: 851000 → 1000, ReleaseCondition: None → None, ChallengeCondition: DefeatWorldTreeBoss → None
  - `Dungeon_GrassHard` : RequiredSeconds: 7200 → 60, RecommendedStrength: 1600000 → 1000, ReleaseCondition: DefeatGrassBoss → None, ChallengeCondition: DefeatGrassBossHard → None
  - `Dungeon_ForestHard` : RequiredSeconds: 7200 → 60, RecommendedStrength: 1600000 → 1000, ReleaseCondition: DefeatDarkIslandBoss → None, ChallengeCondition: DefeatForestBossHard → None
  - `Dungeon_VolcanoHard` : RequiredSeconds: 7200 → 60, RecommendedStrength: 1600000 → 1000, ReleaseCondition: DefeatDarkIslandBoss → None, ChallengeCondition: DefeatVolcanoBossHard → None
  - `Dungeon_DesertHard` : RequiredSeconds: 7200 → 60, RecommendedStrength: 1600000 → 1000, ReleaseCondition: DefeatDarkIslandBoss → None, ChallengeCondition: DefeatDesertBossHard → None
  - `Dungeon_SnowHard` : RequiredSeconds: 7200 → 60, RecommendedStrength: 1600000 → 1000, ReleaseCondition: DefeatDarkIslandBoss → None, ChallengeCondition: DefeatSnowBossHard → None
  - `Dungeon_SakurajimaHard` : RequiredSeconds: 7200 → 60, RecommendedStrength: 1600000 → 1000, ReleaseCondition: DefeatDarkIslandBoss → None, ChallengeCondition: DefeatSakurajimaBossHard → None
  - … 3 autres rows ; champs touchés : RequiredSeconds (×18), RecommendedStrength (×18), ReleaseCondition (×18), ChallengeCondition (×18)
